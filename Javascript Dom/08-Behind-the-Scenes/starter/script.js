'use strict';
function calcAge(birthYear){
    const age = 2035-birthYear
    console.log(firstName)
    function printAge() {
        const output = `You are ${age},born in ${birthYear}`
      console.log(output)
    }
    printAge();
   return age
}
const firstName = 'Zakyboss'
calcAge(1991)